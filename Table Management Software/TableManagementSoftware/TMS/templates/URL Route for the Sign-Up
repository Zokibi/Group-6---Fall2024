# urls.py
from django.urls import path
from .views import signup_view

urlpatterns = [
    path('signup/', signup_view, name='signup'),
    path('home/', your_home_view, name='home'),  # Adjust as necessary
]
